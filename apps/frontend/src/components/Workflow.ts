const steps = [
  ['01', 'Сценарий', 'Что вводит пользователь, что отправляется на сервер, что считается успехом и как выглядит ошибка.'],
  ['02', 'Интерфейс', 'Адаптивная сетка, читаемый текст, форма, фокус, блокировка кнопки и сообщения для пользователя.'],
  ['03', 'Сервер', 'Повторная проверка данных, отправка писем, ограничение частых запросов и понятные ответы для фронтенда.'],
  ['04', 'ИИ', 'Использую ИИ для черновиков и проверки идей, но результат проверяю типами, сборкой и ручными тестами.'],
];

export const Workflow = () => `
  <section class="section workflow" aria-labelledby="workflow-title">
    <p class="section-kicker">Как работаю</p>
    <h2 id="workflow-title">Делаю по шагам, чтобы ничего не потерять</h2>
    <div class="workflow-grid">
      ${steps
        .map(
          ([number, title, text]) => `
            <article>
              <strong>${number}</strong>
              <h3>${title}</h3>
              <p>${text}</p>
            </article>
          `,
        )
        .join('')}
    </div>
  </section>
`;
